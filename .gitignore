# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.python-version

# ── Сборка (PyInstaller / Inno Setup) ──
build/
dist/
installer/
*.spec.tmp

# ── Локальные данные ──
TEMP/
projects/
TZ.md
crash.log
*.log

# ── Офлайн-модели honyaku (1.2 ГБ, файлы >100 МБ — GitHub не принимает) ──
# Модели — часть сборки приложения: скачиваются в models/ рядом с exe.
models/

# ── IDE / OS ──
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
